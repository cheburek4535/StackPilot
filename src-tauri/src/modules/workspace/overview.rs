/// Overview — top-level project status display.
/// Aggregates data from other workspace services.
pub struct OverviewService;

impl OverviewService {
    pub fn new() -> Self {
        Self
    }
}
